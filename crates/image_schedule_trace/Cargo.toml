[package]
name = "image_schedule_trace"
version = "0.1.0"
edition = "2021"
description = "Low-overhead image pipeline schedule tracing (queues, slots, phases)"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
