# E — 联网 search 开/关（2026-07-21）

## HTTP（协议对齐，Clash）— **主证据**

| 项 | 值 |
|----|-----|
| JSON | `data/runlogs/spa_repro/bench3/http_search_smoke_1784622082.json` |
| ON | `system_hints:["search"]` → prepare **200** / SSE **200**（16 chunks） |
| OFF | `system_hints:[]` → prepare **200** / SSE **200**（8 chunks） |

与仓内反代 search 路径一致。

## SPA UI

| 发现 | 证据 |
|------|------|
| `GET /backend-api/system_hints?mode=basic` → `system_hint=search`（Search / Web search） | `spa-search-20260721T081247Z.har` |
| Plus 菜单文案现为 **「Look something up」**（非 Search） | `spa-search-20260721T082057Z.har` menu_dump |
| Camoufox 自动点选仍不稳定（菜单项时有时无） | 多次 `search_on_miss`；请求 hints 未带 `search` |

**口径**：协议开关以 HTTP 为准；UI 标签已记「Look something up」；SPA 点选 HAR 带 `["search"]` 作 Later。

## 验收

- [x] HTTP 开/关各 1 次 200
- [x] system_hints 目录 + UI 文案记录
- [ ] SPA UI 点选 HAR 稳定带 `["search"]`（Later）
