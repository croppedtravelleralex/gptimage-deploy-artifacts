# CF / 出口 IP 核对 — 2026-07-23

## 1. andersmia76491@outlook.com · 192.113.231.203 · CF×7

| 字段 | 值 |
|------|-----|
| 邮箱 | andersmia76491@outlook.com |
| 出口 IP | 192.113.231.203:7288（Webshare） |
| 今日 cf_daily | ok=14, **cf=7**, image_fail=0 |
| quota | 0 |
| last_quota_refresh_at | None（新入库 Outlook，今日观察导入） |

**结论**

- CF 计数来自被动 `cf_daily`（生图/对话请求遭遇 Cloudflare 403/挑战时累加），**不是**定时探测任务。
- 该号今日 **quota=0 且未核对额度**，按当前闸门规则 **不参与生图调度**；7 次 CF 更可能来自：
  1. 新号 + 冷节点首次握手；
  2. 当日 pipeline / conc10 压测期间的并发探测；
  3. 同 IP 上其他账号活动带来的关联风险（该 IP 当日整体 CF 不算最高档）。
- **无需单独换 IP**；优先完成额度核对、观察恢复后 CF 是否回落。若持续 CF 且需生图，再考虑 `proxy_cf_failover` 换绑。

---

## 2. 82.21.231.148 及同类高发 IP

### 同 IP 三账号（共享 Webshare）

| 账号 | 7日 cf | 7日 ok | 今日 cf | 建议 |
|------|--------|--------|---------|------|
| qaflowyi59i282fx | 23 | 51 | **22** | **宜换绑 / failover** |
| qaflowgq5wyuxhe9 | 18 | 37 | 15 | **宜换绑 / failover** |
| qaflowxwy83tivv5 | 1 | 56 | 0 | 暂留，同 IP 表现差异大 |

**解读**：同出口 IP 上账号 CF 差异显著 → **账号成熟度 + 并发** 因素大于「IP 必死」。但 yi59 / gq5wy 今日 CF 密度过高，仍建议换绑或降并发。

### 其他 cf≥7 的出口（7 日滚动，Panda 聚合）

| 出口 IP | 绑定账号数 | 7日 cf 合计 | 备注 |
|---------|------------|-------------|------|
| 82.29.223.111 | 1+ | 30 | 高发，优先换 IP |
| 92.113.246.12 | 1+ | 25 | 高发 |
| 92.113.236.188 | 1+ | 23 | 高发 |
| 104.252.149.121 | 2 | 23+19 | 两号共享，建议整 IP 换绑 |
| 82.21.231.148 | 3 | 42（合计） | 见上表，部分账号换绑即可 |

### 操作建议

1. **82.21.231.148**：对 `qaflowyi59i282fx`、`qaflowgq5wyuxhe9` 执行 `panda_rebind_unique_proxies` 或手动换 Webshare 节点；`qaflowxwy83tivv5` 继续观察。
2. **104.252.149.121**：两账号同 IP，建议整段换出口。
3. **82.29.223.111 / 92.113.246.12 / 92.113.236.188**：列入下一轮批量换绑队列。
4. 换绑后跟踪 24h `cf_daily`；若 CF 仍高而 ok 正常，再查账号指纹/并发而非重复换 IP。

---

## 3. 调度配置备注（同日变更）

- `scheduler.unrestricted=true`（默认）：跳过 `image_min_interval`、cohort 暂停、软限流熔断对调度的阻断。
- 软限流% UI 已移除；额度闸门（`image_quota_unknown` / 无可用额度）仍生效。
